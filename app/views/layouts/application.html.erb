<!DOCTYPE html>
<html>
  <head>
    <title>CodespacesTryRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/daisyui@2.14.1/dist/full.css" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= hotwire_livereload_tags if Rails.env.development? %>
  </head>

  <body>
    <nav class="bg-white p-4 shadow-lg">
      <div class="container mx-auto flex justify-between items-center">
        
        <div class="flex items-center space-x-4">
          <% if user_signed_in? %>
            <%= link_to "Open New Complaint", root_path, class: "btn btn-primary text-white-800 hover:text-gray-600 hover:underline" %>
            <%= link_to "Assistants", assistants_path, class: "text-gray-800 hover:text-gray-600 hover:underline" %>

            <%= link_to "Profile", edit_user_registration_path, class: "text-gray-800 hover:text-gray-600 hover:underline" %>
            <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-error btn-sm" %>
          <% else %>
            <%= link_to "Account", new_user_session_path, class: "btn btn-primary btn-sm" %>
          <% end %>
        </div>
      </div>
    </nav>
  <div class="container mx-auto px-4 py-4">
    <%= render 'shared/flash' %>
    <%= yield %>
  </div>
  </body>
</html>
